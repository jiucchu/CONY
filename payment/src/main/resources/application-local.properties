# ==========================================
# 1. Local-DB Connection Settings (MySQL)
# ==========================================
spring.datasource.url=jdbc:mysql://localhost:3306/cony_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=zhsl106
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================================
# 2. Local-JPA & Hibernate Settings
# ==========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ==========================================
# 3. Redis Configuration (Optional - for TID storage)
# Currently commented out - uncomment when needed
# ==========================================
# spring.redis.host=localhost
# spring.redis.port=6379

# ==========================================
# 4. Kakao Pay API Configuration
# ==========================================
# Admin Key (Secret key ??)
kakao.pay.admin-key=DEV71D4FE841FCDF93B24FAB0626CF0792041C0B
kakao.pay.cid=TC0ONETIME

# API URLs
kakao.pay.ready-url=https://open-api.kakaopay.com/online/v1/payment/ready
kakao.pay.approve-url=https://open-api.kakaopay.com/online/v1/payment/approve

# Redirect URLs (payment ?? ??)
kakao.pay.approval-redirect=http://localhost:8081/api/payments/approve
kakao.pay.cancel-redirect=http://localhost:8081/api/payments/cancel
kakao.pay.fail-redirect=http://localhost:8081/api/payments/fail